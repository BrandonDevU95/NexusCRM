# Backend modules

This folder contains the domain boundaries of the modular monolith.

Rule of thumb: if one change needs to touch too many modules at once, a domain event, contract or boundary is probably missing.

